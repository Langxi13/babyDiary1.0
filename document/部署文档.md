# 部署文档

本文档描述当前统一运行时的构建、数据库迁移、部署、验证和回滚。示例路径与域名均为占位值，真实数据库密码、JWT、邀请码、AI 密钥、站点域名和服务器地址只允许写入服务器私有文件。

## 运行结构

```text
/opt/baby-diary/
├── backend/                         # 后端源码与构建目录
├── frontend/                        # 前端源码与构建目录
├── config/application-prod.yml      # 非秘密生产配置
├── data/objects/                    # 私有统一媒体对象
├── deploy/backend/                  # 当前 Jar 与上一版 Jar
├── deploy/frontend/                 # Nginx 托管的前端产物
├── backups/                         # 权限受限的本地备份
└── scripts/
```

当前服务只运行 `com.langxi.babydiary.BabyDiaryApplication`，API 基地址为 `/api/v3`，服务端默认监听 `127.0.0.1:10002`。旧 API、旧实体/Mapper 和旧 `/images/**` 路由不进入运行制品。

## 环境要求

- Linux、systemd、Nginx
- JDK 17、Maven 3.9+、Node.js 22 LTS、npm
- MySQL 8.0+，推荐 MySQL 8.4 LTS
- Redis 7+；生产默认回环地址 `127.0.0.1:6380`
- 可选：FFmpeg/ffprobe 和支持中文的 PDF 字体

服务器约 4 CPU、3.6 GiB RAM 时，构建、迁移和 E2E 必须串行执行。不要在生产机同时运行 Maven、Gradle 和 Playwright；不要在迁移期间运行压测。

## 私有配置

生产私有环境文件为 `/etc/baby-diary/backend.env`，权限必须为 `600`，目录权限为 `700`。最小配置如下：

```bash
V3_DB_URL='jdbc:mysql://127.0.0.1:3306/baby_diary_v3?connectionTimeZone=UTC&forceConnectionTimeZoneToSession=true&preserveInstants=true&useSSL=false&allowPublicKeyRetrieval=true'
V3_DB_USERNAME=baby_diary_app
V3_DB_PASSWORD=replace-with-private-password
MYSQL_DATABASE=baby_diary_v3
DB_USERNAME=baby_diary_app
DB_PASSWORD=replace-with-private-password
SERVER_ADDRESS=127.0.0.1
DIARY_FILE_PATH=/opt/baby-diary/data/images/
DIARY_OBJECT_PATH=/opt/baby-diary/data/objects/
JWT_SECRET=replace-with-private-secret
JWT_ACCESS_EXPIRATION=900000
AUTH_REFRESH_DAYS=30
AUTH_SECURE_COOKIE=true
INVITATION_CODE_ENCRYPTION_KEY=replace-with-private-key
AI_CONFIG_ENCRYPTION_KEY=replace-with-private-key
CORS_ALLOWED_ORIGINS=https://diary.example.com
CORS_NATIVE_ORIGINS=https://localhost,capacitor://localhost
REDIS_HOST=127.0.0.1
REDIS_PORT=6380
OBJECT_STORAGE_PROVIDER=local
HEALTH_CHECK_BASE_URL=https://diary.example.com
HEALTH_CHECK_RESOLVE_HOST=diary.example.com:443:127.0.0.1
SPRINGDOC_ENABLED=false
```

当前数据库已完成统一；只有没有任何账户的全新库才使用一次性 `INVITATION_CODE` 引导初始化。`V3_DB_*` 必须明确指向生产统一数据库。

生成密钥：

```bash
openssl rand -base64 48
install -d -m 700 /etc/baby-diary
install -m 600 /dev/null /etc/baby-diary/backend.env
```

禁止把该文件、数据库 dump、媒体对象、日志或备份放入 Git。

## 构建与自动验证

先确认工作区和网络正常，再按顺序执行：

```bash
cd /opt/baby-diary
source scripts/java-env.sh
mvn "${MAVEN_SETTINGS_ARGS[@]}" -q -DskipTests clean package -f backend/pom.xml
unzip -p backend/target/Baby-Diary-0.0.1-SNAPSHOT.jar META-INF/MANIFEST.MF | grep 'Start-Class: com.langxi.babydiary.BabyDiaryApplication'
npm --prefix frontend run test:unit
npm --prefix frontend run build
```

发布候选还应串行执行 `scripts/verify.sh`、`scripts/verify-e2e.sh` 和 `scripts/security-scan.sh`。资源受限时可先执行后端验证，再执行前端和 E2E；详细门禁见 [测试与发布验收方案](测试与发布验收方案.md)。

## 数据库备份与迁移

每次发布前先生成并验证当前统一数据库的恢复包：

```bash
scripts/backup.sh
scripts/verify-backup.sh backups/<timestamp>
```

备份脚本与运行时使用相同的数据库选择规则：优先读取 `V3_DB_URL`、`V3_DB_USERNAME` 和 `V3_DB_PASSWORD`，未单独设置密码时与 Spring 配置一致回退到 `DB_PASSWORD`。MySQL 主机、端口和库名从 JDBC URL 或显式 `MYSQL_*` 覆盖项取得。

备份目录权限必须为 `0700`，文件权限必须为 `0600`。恢复包只包含压缩数据库、LOCAL 对象归档、发布清单、校验和以及存在时的 systemd、Nginx、私有环境和 Android 签名材料；源码、Git 历史、部署产物和 `.private/` 不重复进入备份。完成后应复制到服务器外的受保护介质。

Flyway 目录只包含已执行的 V1/V2 和后续连续版本迁移。不得修改已执行迁移；新增迁移需先通过 Testcontainers 空库测试、备份恢复演练和预发布升级测试。生产启动会自动校验 Flyway checksum，校验失败时停止发布，不使用 `repair` 掩盖差异。

## systemd、Redis 与存储

`scripts/deploy.sh` 会安装 systemd runtime drop-in，覆盖服务的 Jar 和配置入口：

```ini
[Service]
ExecStart=
ExecStart=/usr/bin/java -Xms256m -Xmx768m -jar /opt/baby-diary/deploy/backend/Baby-Diary-0.0.1-SNAPSHOT.jar --spring.config.location=file:/opt/baby-diary/config/application-prod.yml
```

后端使用专用 `baby-diary` 用户运行，启用 `PrivateTmp=true`，只绑定回环地址。Redis 故障时应用会降级到本地有界限流和数据库读取，但生产仍应恢复 Redis，避免长期增加数据库压力。统一媒体对象目录应为独立的 `0700` 目录，对象文件为 `0600`；不要让 Nginx 直接 alias 该目录。

## Nginx

应用站点至少应包含以下路由：

```nginx
root /opt/baby-diary/deploy/frontend;
client_max_body_size 300M;
include /etc/nginx/snippets/baby-diary-security-headers.conf;

location / { try_files $uri $uri/ /index.html; }
location ^~ /api/ {
    proxy_pass http://127.0.0.1:10002/api/;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}
include /etc/nginx/snippets/baby-diary-backend-health.conf;
location = /manifest.webmanifest {
    default_type application/manifest+json;
    try_files $uri =404;
}
```

不要添加旧 `/images/**` 静态 alias。V3 媒体通过带签名的 `/api/v3/public/media/**` 读取，资源策略只对该路径设置 `cross-origin`，其他同源资源保持默认策略。

## 发布与健康检查

```bash
scripts/deploy.sh
scripts/health-check.sh
```

部署顺序为 5 GiB 磁盘门禁、构建、运行治理检查、`nginx -t`、停止服务、备份上一版 Jar、替换 Jar、同步前端、启动服务、重载 Nginx、健康检查。健康检查必须通过：

- SPA 首页、日记和相册返回 `200`；
- `/api/v3/client/bootstrap` 返回 `200`；
- 未登录 `/api/v3/account/profile` 返回 `401`；
- `/actuator/health` 返回 JSON 且顶层 `status` 为 `UP`；
- `diary-backend` 和 Nginx 均为 active；
- 日志无 Flyway 失败或新增未预期 5xx。

生产冒烟只使用现有账户和只读请求；登录、相册、日记列表/详情、头像、纪念日、AI 配置读取和移动端 bootstrap 应人工确认。

## 回滚与保留策略

切换后的观察窗口至少 14 天。期间保留：旧 `baby-diary` 数据库、旧媒体目录、V3 目标库、上一版 Jar 和切换前备份。发生启动失败、数据数量不一致、跨空间可见、媒体无法读取或连续 5xx 时：

1. 停止 V3 服务。
2. 恢复旧 systemd drop-in、旧 Jar 和旧 `DB_URL`。
3. 启动旧服务并运行旧版本健康检查。
4. 保留 V3 目标库和日志，不直接删除，待定位后再决定是否重新迁移。

确认观察窗口结束且备份已在服务器外验证后，才可删除旧库、旧目录和旧 Jar。删除前再次执行隐私审查和磁盘用量确认。
