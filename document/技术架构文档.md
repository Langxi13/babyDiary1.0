# 技术架构文档

## 目录

- [系统架构概述](#系统架构概述)
- [后端技术栈](#后端技术栈)
- [前端技术栈](#前端技术栈)
- [数据库设计](#数据库设计)
- [系统分层架构](#系统分层架构)
- [核心模块设计](#核心模块设计)
- [安全设计](#安全设计)

---

## 系统架构概述

### 整体架构

Baby-Diary系统采用前后端分离架构，后端只提供RESTful API服务，前端由Nginx托管Vue/Vite构建产物。

```
┌─────────────────┐         HTTP/HTTPS         ┌─────────────────┐
│   前端应用      │ ◄─────────────────────────► │   后端服务      │
│  (Vue 3 + Vite) │                             │ (Spring Boot)   │
└─────────────────┘                             └─────────────────┘
         │                                               │
         ▼                                               ▼
┌─────────────────┐                             ┌─────────────────┐
│   浏览器        │                             │   MySQL 数据库   │
│  (用户界面)     │                             └─────────────────┘
└─────────────────┘                                      │
                                                        ▼
                                               ┌─────────────────┐
                                               │ Redis 缓存      │
                                               └─────────────────┘
```

### 架构特点

1. **前后端分离**：前端和后端完全独立开发、部署和维护
2. **RESTful API**：后端提供标准的RESTful接口
3. **混合会话认证**：短期 JWT 负责接口认证，服务端保存并轮换刷新会话
4. **分层设计**：后端采用经典的三层架构（Controller-Service-Mapper）
5. **REST-only**：旧Thymeleaf页面和后端模板Controller已移除
6. **移动端 App Shell**：Vue 前端在手机宽度下由全局 Shell 统一提供顶部栏、底部主导航和安全区布局，页面只负责业务内容
7. **安卓系统分享入口**：安装后的 PWA 通过 Web App Manifest `share_target` 注册为图片分享目标，安卓用户可从系统相册把图片分享到 Baby Diary，再进入写日记页继续编辑
8. **空间化数据模型**：个人空间与共同空间共用 V2 日记、协作、同步、媒体和 AI 能力
9. **离线优先写入**：IndexedDB 保存待同步操作，服务端以幂等操作 ID 和版本号处理冲突
10. **分层媒体存储**：旧版图片保留静态兼容路径，V2 富媒体使用私有本地目录或 S3 兼容对象存储和短时签名 URL

---

## 后端技术栈

### 核心框架

| 技术 | 版本 | 说明 |
|------|------|------|
| Spring Boot | 3.5.16 | 核心应用框架 |
| Spring Security | 6.x | 安全认证框架 |
| MyBatis Spring Boot Starter | 3.0.5 | ORM持久层框架 |
| MySQL | 8.0+ / 8.4 LTS | 关系型数据库 |
| Redis | 7.x | 生产读缓存 |

### 主要依赖

| 依赖 | 版本 | 说明 |
|------|------|------|
| JWT (JJWT) | 0.12.6 | JWT Token生成和验证 |
| springdoc-openapi | 2.8.17 | API文档生成 |
| Spring Boot Actuator | - | 运行健康检查 |
| Thumbnailator | 0.4.20 | 图片压缩处理 |
| Flyway | - | 数据库迁移 |
| Spring Data Redis | - | Redis Cache 接入 |
| Jsoup | 1.17.2 | 富文本 HTML 白名单清洗 |
| RestTemplate | - | OpenAI 兼容接口调用 |
| MinIO SDK | 8.6.0 | S3 兼容对象存储 |
| web-push | 5.1.2 | VAPID Web Push 投递 |
| jose4j | 0.9.6 | Web Push VAPID/JWT 加密支持 |
| Bouncy Castle | 1.84 | Web Push 与对象存储加密基础 |
| AsyncHttpClient | 2.15.0 | Web Push 网络客户端 |
| OpenHTMLtoPDF | 1.0.10 | PDF 日记书导出 |
| Lombok | - | 简化Java代码 |

### 开发工具

| 工具 | 说明 |
|------|------|
| Maven | 项目构建和依赖管理 |
| JDK | 17 LTS |

---

## 前端技术栈

### 核心框架

| 技术 | 版本 | 说明 |
|------|------|------|
| Vue | 3.x | 前端框架 |
| Vite | 7.3.x | 构建工具 |
| Vue Router | - | 路由管理 |
| Pinia | - | 状态管理 |
| Element Plus | - | UI组件库 |
| Axios | - | HTTP客户端 |
| Tiptap | 3.x | 日记富文本编辑器 |
| Capacitor | 8.4.x | Android/iOS 原生容器、HTTP/Cookie 桥和平台插件 |

### 开发工具

| 工具 | 说明 |
|------|------|
| Node.js | 运行环境 |
| npm | 包管理器 |

### 构建与拆包

前端页面通过 Vue Router 懒加载。日记编辑页使用 Tiptap，相关 ProseMirror 代码只进入 `DiaryForm` 路由 chunk，不进入首页首屏预加载。Element Plus 组件按页面局部引入，避免全量 UI 库进入主包。桌面导航 hover、手机导航 touchstart 时会预加载目标路由 chunk，降低首次切换页面的等待时间。

前端 API 层提供内存 TTL 缓存和同 key 请求去重。列表、首页、相册和草稿默认缓存 30 秒，时间轴和日历默认缓存 2 分钟，标签和纪念日默认缓存 10 分钟；创建、更新、删除、收藏等写入操作会失效相关缓存。所有缓存键自动增加当前用户作用域，避免不同账号复用相同业务键。缓存和 HTTP 请求同时记录客户端会话 generation：退出、登录其他账号或跨标签页身份变化时统一递增 generation、清空缓存和认证相关 Store；旧请求、旧刷新操作完成后会作为取消请求丢弃，不能写入新会话。会话重置还会中止在途刷新请求，同一标签页的新登录会等待服务端登出完成，避免旧响应覆盖新账号的刷新 Cookie。共同空间离线数据不在退出时删除，但待同步计数只统计当前账号可访问的空间。

敏感操作通过 `stepUp.js` 复用一个挂载在应用根节点的 `StepUpDialog`。调用方只等待验证 Promise，不直接依赖具体 UI 组件；弹窗负责并发请求合并、密码提交、原位错误、取消和会话重置。认证 API 在弹窗打开后动态加载，避免将低频认证管理代码并入首屏主包；步骤验证请求使用局部静默错误配置，由弹窗呈现可恢复错误，访问令牌失效等全局认证处理仍由统一响应拦截器接管。

页面级 SCSS 从 `DiaryForm`、`AiReports`、`Anniversaries`、`Album`、`AlbumDetail`、`DiaryList`、`Timeline`、`Home` 和移动壳组件中拆出到独立样式文件。日记图片选择、安卓上传兜底和 PWA 分享导入集中在 `useDiaryImages` composable，降低单个 SFC 的维护规模。全局按钮和页面过渡只声明实际变化的 CSS 属性，不使用 `transition: all` 触发布局无关动画。

`frontend/src/assets/styles/mobile-foundation.scss` 作为手机端公共布局层，集中定义顶部栏、底部栏、页面边距、表面颜色和控件圆角变量。在 `768px` 及以下统一提供安全区内边距、最小 `320px` 布局基线、横向溢出保护、输入控件 16px 字号与至少 44px 触控高度，并约束 Element Plus 的对话框、消息、确认框、日期面板和下拉面板不超出动态视口。页面样式只保留各业务区域自身的网格和视觉层级，减少重复的移动端基础规则。

### PWA、Capacitor 与移动壳能力

`frontend/public/manifest.webmanifest` 提供安装元数据，并通过 `share_target` 注册 `/share-target` 作为安卓系统图片分享入口。`frontend/public/sw.js` 拦截该入口的 `POST multipart/form-data` 请求，只接受 10MB 内的 JPEG、PNG、GIF、WebP 文件，写入专用 Cache API 命名空间 `baby-diary-share-target-v1`，再重定向到 `/diaries/create?shared=1`。普通 GET 只缓存同源成功响应；网络失败时只有页面导航请求回退到 `index.html`，静态资源失败不会再错误返回 HTML。写日记页通过 `shareTargetFiles.js` 消费缓存文件，最终仍走创建日记接口的 `imageFiles` 字段。

`MobileAppShell` 将手机端顶部栏、五项主导航和次级功能抽屉与桌面 `NavBar` 分离。桌面导航从共享入口配置中展示首页、日记、空间、相册和写日记，其余功能进入“更多”菜单；主导航和抽屉入口在触摸开始时预加载路由 chunk。抽屉打开时锁定页面滚动并支持 Escape 关闭。壳层监听 `visualViewport` 高度变化，识别软键盘后隐藏底部导航，避免导航遮挡输入区。日记列表在手机端使用两个独立的单日期选择器，避免双日历面板在窄屏并排溢出。

Capacitor 8 客户端复用同一套 Vue 路由和组件；原生环境使用 hash history，首次启动进入 `ServerSetup`，只把通过 `/api/v2/client/bootstrap` 验证的 HTTPS 根地址保存在 Capacitor Preferences。远端图片 URL 由 `runtimeConfig` 统一补全，Web/PWA 继续使用相对路径。切换服务器会先清除访问令牌、账号信息、API 缓存、IndexedDB 离线队列和原生 Cookie，避免跨服务器复用旧会话。

原生登录、刷新和退出使用 `CapacitorHttp`，刷新令牌仍由后端以 `Secure`、`HttpOnly` Cookie 管理；普通 JSON 与 multipart 业务请求继续走 Axios 和 Bearer Token。Android WebView 来源固定为 `https://localhost`，iOS 为 `capacitor://localhost`，后端只允许这两个明确来源。Nginx 对普通资源保持 `Cross-Origin-Resource-Policy: same-origin`，仅对 `/images/**` 和短时签名媒体路径返回 `cross-origin`，使原生 WebView 可以显示图片而不放宽其他资源。

`NativeImageActions` 封装系统相册和相机，覆盖日记、头像、纪念日封面和共同空间图片入口。受支持的 JPEG、PNG、GIF、WebP 保留格式；HEIC/HEIF 等格式使用原生 JPEG 预览或 Android 解码转码后上传。Android `NativeShareReceiverPlugin` 接收 `ACTION_SEND`/`ACTION_SEND_MULTIPLE`，把最多 20 个、单项 10MB 内的图片复制到私有缓存，24 小时后清理；单个坏文件不会导致整批失败。应用关闭系统备份和设备迁移，分享缓存不进入公共外部存储。

Android 工程位于 `frontend/android`，使用 JDK 21、SDK 36、单 Gradle worker 和无常驻 daemon；后端继续使用 JDK 17。CI 生成 Debug APK，手动工作流使用 GitHub Secrets 生成签名 APK/AAB 并校验签名。iOS 依赖已锁定，平台工程、签名和真机验收等待 Mac/Xcode 环境完成。

PWA Web Share Target 仍保留给安装后的 Android Chrome PWA；普通浏览器继续使用标准文件选择入口。共同空间编辑器另使用 IndexedDB 保存离线日记和媒体队列，并通过 V2 sync pull/push 接口增量同步。

生产敏感配置不写入仓库。后端默认配置位于 `backend/src/main/resources/application.yml`，生产覆盖配置位于 `config/application-prod.yml`，真实密钥和数据库连接从 `/etc/baby-diary/backend.env` 注入。生产 Java 进程使用专用系统用户 `baby-diary` 运行，数据库连接使用业务账号 `baby_diary_app`，不使用 root 作为应用运行账号。

---

## 数据库设计

### 数据库表结构

#### user表（用户表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| user_id | INT | 用户ID | 主键，自增 |
| username | VARCHAR | 用户名 | 唯一，非空 |
| password | VARCHAR | 密码（加密） | 非空 |
| avatar_path | VARCHAR | 头像路径 | 可空 |
| created_at | TIMESTAMP | 创建时间 | 默认当前时间 |
| email | VARCHAR | 登录恢复邮箱 | 唯一，可空 |
| email_verified | TINYINT | 邮箱是否验证 | 非空 |
| token_version | INT | JWT 全局失效版本 | 非空 |
| system_role | VARCHAR | USER/ADMIN | 非空 |
| timezone | VARCHAR | 用户时区 | 默认 Asia/Shanghai |

#### diary表（日记表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| diary_id | INT | 日记ID | 主键，自增 |
| user_id | INT | 用户ID | 外键，非空 |
| title | VARCHAR | 日记标题 | 非空 |
| date | DATE | 日记日期 | 非空 |
| content | MEDIUMTEXT | 日记内容 | 非空 |
| created_at | TIMESTAMP | 创建时间 | 默认当前时间 |
| public_id | CHAR(36) | 对外稳定 ID | 唯一 |
| space_id | BIGINT | 所属空间 | 外键 |
| version | INT | 乐观锁版本 | 非空 |
| visibility | VARCHAR | SHARED/PRIVATE | 非空 |
| locked | TINYINT | 是否启用日记锁 | 非空 |
| deleted_at | TIMESTAMP | 回收站时间 | 可空 |

#### diary_image表（日记图片表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| image_id | INT | 图片ID | 主键，自增 |
| diary_id | INT | 日记ID | 外键，非空 |
| image_path | VARCHAR | 图片路径 | 非空 |
| sort | INT | 排序 | 非空 |
| created_at | TIMESTAMP | 创建时间 | 默认当前时间 |

#### ai_config表（AI配置表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| config_id | INT | 固定配置ID | 主键 |
| enabled | TINYINT | 是否启用 AI | 非空 |
| base_url | VARCHAR | OpenAI 兼容接口 Base URL | 可空 |
| model | VARCHAR | 模型名称 | 可空 |
| encrypted_api_key | TEXT | 加密后的 API Key | 可空 |
| timeout_seconds | INT | 请求超时时间 | 非空 |
| updated_at | TIMESTAMP | 更新时间 | 自动更新 |

#### ai_report表（AI报告表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| report_id | INT | 报告ID | 主键，自增 |
| user_id | INT | 用户ID | 外键，非空 |
| type | VARCHAR | 报告类型：WEEKLY/MONTHLY | 非空 |
| period | VARCHAR | 周期标签，如 2026-W27 或 2026-07 | 非空 |
| period_start | DATE | 周期开始日期 | 非空 |
| period_end | DATE | 周期结束日期 | 非空 |
| title | VARCHAR | 报告标题 | 非空 |
| content_markdown | MEDIUMTEXT | Markdown 报告内容 | 非空 |
| diary_count | INT | 本期日记数量 | 非空 |
| model | VARCHAR | 生成时使用的模型 | 可空 |
| created_at | TIMESTAMP | 创建时间 | 默认当前时间 |

#### system_invitation_config表（系统邀请码配置表）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| config_id | INT | 固定配置ID，值为1 | 主键 |
| encrypted_code | TEXT | AES-GCM 加密后的邀请码 | 非空 |
| updated_by | INT | 最近轮换管理员ID | 可空 |
| updated_at | TIMESTAMP | 最近更新时间 | 自动更新 |

### 数据库关系

- user (1) ----< (N) auth_session / account_token / recovery_code
- user (N) >----< (N) diary_space，通过 space_member 关联
- diary_space (1) ----< (N) diary / tag / media_asset / notification / reminder
- diary (1) ----< (N) diary_revision / diary_comment / diary_reaction / diary_media
- diary (1) ----< (N) diary_image
- diary_space (1) ----< (N) ai_report / space_ai_schedule / private_share / sync_change

V9-V12 新增或扩展的核心表包括 `diary_space`、`space_member`、`space_invitation`、`auth_session`、`account_token`、`account_recovery_code`、`diary_revision`、`diary_comment`、`diary_reaction`、`app_notification`、`push_subscription`、`sync_change`、`sync_operation_result`、`diary_search_index`、`diary_template`、`media_asset`、`diary_media`、`space_storage_usage`、`private_share`、`space_ai_schedule` 和 `reminder`。

### 查询与缓存优化

- 日记列表支持 `summary=true`，只读取 `LEFT(content, 512)` 作为预览，详情和编辑接口仍读取完整正文。
- 日记列表、时间轴和草稿列表只读取正文摘要；详情、编辑和单份草稿接口仍读取完整正文。
- 日记列表和时间轴按当前结果 diaryId 批量查询标签和图片，避免每篇日记单独查询。
- Flyway V4 增加 `diary(user_id,date,created_at)`、`diary(user_id,mood_key,date)`、`diary_image(diary_id,sort,image_id)` 和 `diary_tag(tag_id,diary_id)` 索引。
- Flyway V5 增加 `ai_config` 和 `ai_report`，用于保存 OpenAI 兼容配置和周报/月报历史。
- Flyway V7 增加 `album_photo(album_id,sort,image_id)` 和 `ai_report(user_id,created_at,report_id)`，覆盖相册详情和 AI 报告历史排序查询；相册详情排序收敛为 `album_photo.sort,image_id`，避免跨表排序 filesort。
- Flyway V8 将 `diary.content` 扩展为 `MEDIUMTEXT`，与富文本日记和草稿容量保持一致。
- Flyway V9 建立空间身份、公开 UUID、可见范围、日记锁、版本号、回收站、刷新会话和账户恢复基础。
- Flyway V10 增加修订、评论、回应、通知、Web Push、同步日志和幂等操作结果。
- Flyway V11 增加中文搜索索引、模板、富媒体、空间用量、私密分享和空间 AI 定时任务。
- Flyway V12 为提醒投递增加领取与重复投递保护字段。
- Flyway V13 将 `diary_image.image_path` 与 `album.cover_image_path` 统一为 `utf8mb4_bin`，保证升级自旧库后封面路径仍可进行精确等值比较。
- 通用照片、系统相册和自建相册详情提供数据库 `LIMIT/OFFSET` 分页；相册首批默认24张，元数据和首批照片并行请求，首页收藏预览只读取6张，避免照片量增长后一次返回和解码全部图片。
- 生产环境启用 Spring Cache + Redis，缓存命名空间前缀为 `baby-diary:prod:`，读缓存 key 均包含 userId 或用户作用域参数。
- Redis 读写或清理异常由 `CacheErrorHandler` 降级处理，业务查询继续回源数据库，避免缓存服务成为页面可用性的单点。
- 缓存名称统一由 `CacheNames` 管理：`diaryPage` 30秒、`diaryTimeline` 2分钟、`diaryCalendar` 2分钟、`tags` 10分钟、`anniversaries` 10分钟、`photos` 30秒、`drafts` 30秒。
- 相册组摘要、系统照片分页和自建相册分页复用 `photos` 缓存；日记图片、收藏和相册写操作统一清空该缓存。
- 时间轴和日历查询使用日期范围条件，不在 WHERE 中使用 `YEAR(date)` 或 `MONTH(date)`，以便复用日期索引。
- 相册封面回退策略不再使用 `ORDER BY RAND()`，改为按相册照片排序稳定选择已有图片；后续数据规模继续增大时可扩展为持久化 `cover_image_id`。
- 前端缩略图 URL 统一由 `thumbnailImageUrl()` 生成，列表、相册、首页、时间轴等小图区域读取 `/images/thumbs/480/`，全屏预览和导出继续读取原图，降低首屏图片传输体积。
- 日记列表 Store 使用递增请求序号，只允许最后一次筛选请求更新页面；关键词输入使用350毫秒防抖，减少无效查询并避免旧响应覆盖新筛选。
- `PageResult.map()` 统一分页实体到 VO 的转换，照片和 AI 报告控制器不再重复组装分页元数据。
- `/api/**` 注册慢请求日志拦截器，生产默认阈值 `SLOW_REQUEST_THRESHOLD_MS=1000` 毫秒。

---

## 系统分层架构

### 后端分层

```
┌─────────────────────────────────────────────────┐
│                   Controller层                    │
│  (处理HTTP请求，参数校验，返回响应)               │
└──────────────────────┬──────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────┐
│                    Service层                      │
│  (业务逻辑处理，事务管理)                         │
└──────────────────────┬──────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────┐
│                   Mapper层                       │
│  (数据访问，SQL执行)                              │
└──────────────────────┬──────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────┐
│                   MySQL数据库                     │
└─────────────────────────────────────────────────┘
```

### 层职责说明

#### Controller层

- 接收HTTP请求
- 参数校验和绑定
- 调用Service层处理业务
- 封装响应结果返回

主要Controller：
- `AuthController`：认证相关接口
- `DiaryApiController`：日记管理接口
- `PhotoController`：图片管理接口
- `AlbumController`：相册和相册组接口
- `AiController`：AI 配置和周报/月报接口

#### Service层

- 业务逻辑处理
- 事务管理
- 调用Mapper层访问数据
- 处理业务规则

主要Service：
- `LoginService`：用户认证服务
- `DiaryService`：日记管理服务
- `DiaryImageService`：日记图片服务
- `DiaryArchiveService`：图片 ZIP 导出和临时文件生命周期
- `ImageStorageService`：统一图片校验、随机命名、缩略图和事务清理
- `AiConfigService`：AI 配置读取、保存和连接测试
- `AiReportService`：报告周期解析、日记聚合、AI 调用和报告保存

#### Mapper层

- 数据库操作
- SQL定义和执行
- 数据映射

主要Mapper：
- `UserMapper`：用户数据访问
- `DiaryMapper`：日记数据访问
- `DiaryImageMapper`：日记图片数据访问
- `AiConfigMapper`：AI 配置数据访问
- `AiReportMapper`：AI 报告历史数据访问

### 后端模块边界

当前阶段先治理边界和依赖方向，不做大规模搬包，避免影响已上线功能。后续重构可以按以下模块拆包：

| 模块 | 当前主要类 | 边界约束 |
|------|------------|----------|
| auth | `AuthController`、`LoginService`、`JwtTokenProvider`、`JwtAuthenticationFilter`、`CurrentUser` | 只负责认证、JWT 和当前用户识别，不直接处理日记、相册或 AI 业务 |
| diary | `DiaryApiController`、`DiaryDraftController`、`TagController`、`DiaryService`、`DiaryArchiveService`、`ImageStorageService`、`DiaryDraftService`、`TagService`、`DiaryMapper` | 日记正文、草稿、标签、时间轴、日历、导出和图片生命周期 |
| album | `AlbumController`、`PhotoController`、`AlbumService`、`PhotoService`、`AiAlbumProposalService`、`AlbumMapper`、`PhotoMapper` | 相册组、相册照片索引、系统相册和 AI 相册确认流程 |
| ai | `AiController`、`AiConfigService`、`AiReportService`、`OpenAiCompatibleClient`、`AiConfigMapper`、`AiReportMapper` | AI 配置、模型列表、报告生成和报告历史；不直接返回明文 API Key |
| experience | `AnniversaryController`、`AnniversaryService`、`AnniversaryMapper` | 纪念日和扩展体验功能 |
| workspace | `SpaceController`、`SpaceService`、`CollaborativeDiaryService`、`DiaryInteractionService`、`SpaceMapper`、`CollaborationMapper` | 空间成员、V2 日记生命周期、协作互动和权限边界 |
| sync | `OfflineSyncService`、`SyncOperationExecutor`、`SyncJournalService`、`SyncResultStore` | 增量变更、幂等写入、冲突检测和脱敏同步 |
| media | `MediaService`、`MediaProcessingService`、`ObjectStorage`、`LocalObjectStorage`、`S3ObjectStorage` | 私有对象存储、签名读取、额度、处理任务和事务清理 |
| account | `AuthSessionService`、`AccountSecurityService`、`StepUpTokenVerifier`、`AccountMailService` | 刷新会话、设备、邮箱、恢复码和敏感操作二次验证 |
| admin | `AdminInvitationCodeController`、`InvitationCodeService`、`InvitationCodeCrypto` | 系统管理员邀请码查看、轮换、加密持久化与首次启动导入 |
| common | `Result`、`PageResult`、`Pagination`、`ErrorCode`、`CacheNames`、`GlobalExceptionHandler`、`HtmlSanitizer`、`DiaryRequestValidator` | 统一响应、分页上限、错误码、缓存命名、输入边界和跨模块基础能力 |

接口响应继续统一为 `Result<T>`，分页响应统一为 `PageResult<T>`。新增接口应优先复用这两个结构，不再返回临时 Map 或裸数组作为顶层响应。

---

## 核心模块设计

### 认证模块

#### JWT认证流程

```
1. 用户提交用户名和密码
2. Spring Security验证用户名和密码
3. 验证成功后生成15分钟访问令牌和随机刷新令牌
4. 刷新令牌只以 SHA-256 摘要入库，原值写入 HttpOnly Cookie
5. 前端在后续请求 Header 中携带访问令牌
6. 访问令牌过期时，前端调用刷新接口并轮换刷新令牌
7. 前端刷新操作绑定发起时的会话 generation；账号已退出或切换时，旧刷新结果不得恢复凭据
8. JwtAuthenticationFilter 校验签名、过期时间和用户 tokenVersion
9. 验证通过后设置 SecurityContext 并放行
```

#### 关键类

- `JwtTokenProvider`：Token生成和验证
- `JwtAuthenticationFilter`：Token拦截和验证
- `SecurityConfig`：安全配置
- `CustomUserDetails`：用户详情实现
- `AuthSessionService`：刷新令牌创建、轮换、撤销和设备列表
- `AccountSecurityService`：二次验证、邮箱令牌、密码找回和恢复码
- `StepUpTokenVerifier`：独立校验短期二次验证令牌，避免管理功能反向依赖密码认证配置

### 日记管理模块

#### 创建日记流程

```
1. 前端提交日记数据和图片
2. DiaryApiController接收请求
3. 调用DiaryService.saveDiary()
4. 保存日记基本信息到diary表
5. 获取生成的diaryId
6. 遍历图片文件，压缩处理
7. 保存图片文件到本地
8. 生成 `thumbs/480/{filename}` 缩略图
9. 保存图片信息到diary_image表
10. 返回创建结果
```

#### 图片压缩

使用Thumbnailator库进行图片压缩：

- 压缩阈值：300KB
- 最大尺寸：1920px
- 压缩质量：0.85

关键类：`ImageCompressor`

#### 图片缩略图

后端为每张上传图片维护 480px 缩略图：

- 存储位置：`${DIARY_FILE_PATH}/thumbs/480/`
- 访问路径：`/images/thumbs/480/{filename}`
- 生成策略：创建或追加上传图片后生成缩略图，小图不强制放大
- 清理策略：删除日记或编辑时移除旧图片，会同步删除对应缩略图
- 历史数据：`scripts/generate-thumbnails.sh` 会扫描 `data/images/` 的原图并补齐缺失缩略图

### 图片管理模块

#### 图片存储

- 存储位置：`DIARY_FILE_PATH` 指定的目录
- 缩略图位置：`${DIARY_FILE_PATH}/thumbs/480/`
- 文件名格式：`diary_{userId}_{uuid}.jpg`、`avatar_{userId}_{uuid}.jpg`、`anniversary_{userId}_{uuid}.jpg` 等随机服务端名称
- 支持格式：JPEG、PNG、GIF、WebP
- 文件大小限制：10MB
- 校验方式：同时校验允许的 MIME 和文件签名，不接受 SVG 或伪造图片类型
- 事务规则：数据库回滚时清理本次新文件，旧图片只在事务提交后删除，避免数据库与文件系统状态相反
- 导出规则：ZIP 临时文件在响应流关闭后自动删除

### AI报告模块

#### 生成流程

```
1. 前端选择周报、月报或年报周期，或由空间定时任务触发
2. AiController 校验登录用户和请求参数
3. AiReportService 解析周期，并查询个人或空间在该周期内的可用日记
4. 批量补充日记标签，仅整理日期、标题、文字内容、心情和标签
5. 锁定日记在查询层排除，OpenAiCompatibleClient 调用配置的 /chat/completions 接口
6. 生成 Markdown 报告并写入 ai_report
7. 前端展示报告，并可在历史报告中再次查看或删除
```

#### 配置方式

- 使用 OpenAI 兼容 Chat Completions 风格配置：`baseUrl`、`model`、`apiKey`、`timeoutSeconds`。
- `baseUrl` 填写到版本路径，例如 `https://api.openai.com/v1`，后端自动拼接 `/chat/completions`。
- `apiKey` 只在保存时提交明文，后端使用 `AI_CONFIG_ENCRYPTION_KEY` 派生 AES-GCM 密钥加密后存库，读取配置时只返回脱敏状态。
- AI 输入不包含图片文件、图片地址或图片识别内容。

---

## 安全设计

### 认证安全

1. **密码加密**：使用Spring Security的BCrypt加密算法
2. **访问与刷新会话**：
   - 使用HMAC-SHA256签名算法
   - V2 访问令牌默认15分钟，刷新会话默认30天并单次轮换
   - JWT 包含用户ID、用户名和 tokenVersion；修改密码会使旧令牌失效
   - 刷新令牌、邮箱令牌和二次验证令牌仅以摘要存储
3. **日记锁**：密码二次验证令牌默认10分钟，派生数据和同步载荷同步脱敏
4. **邀请码注册**：全局邀请码使用独立环境密钥派生的 AES-256-GCM 密钥加密存库；注册事务读取当前密文并恒定时间比较，轮换提交后旧码立即失效
5. **管理员查看**：邀请码明文接口同时要求 `ROLE_ADMIN` 和短期二次验证令牌，响应禁止缓存且不记录明文日志

### 接口安全

1. **CORS配置**：生产环境只允许 `CORS_ALLOWED_ORIGINS` 中明确配置的来源
2. **权限控制**：用户只能访问和操作自己的数据
3. **参数校验**：使用 Validation 和 `DiaryRequestValidator` 校验日期、长度、格式、图片数量与分页上限；分页每页最多100条
4. **SQL注入防护**：使用MyBatis参数化查询
5. **健康检查最小暴露**：Actuator 仅开放 `/actuator/health`；Nginx 使用精确 location 代理到回环后端，发布探活同时校验 JSON 类型和顶层 `status=UP`，不接受 SPA fallback 的伪 200
6. **管理接口**：AI 配置和系统邀请码只允许 ADMIN；邀请码查看和轮换额外要求密码二次验证；生产 Swagger/OpenAPI 默认关闭
7. **限流**：登录、刷新、找回密码、恢复码和私密分享入口按来源与身份限流
8. **网络边界**：生产 Java 服务只绑定 `127.0.0.1:10002`，公网流量必须经过提供 TLS、安全头和静态资源服务的 Nginx
9. **运行隔离**：systemd 通过 `PrivateTmp=true` 隔离 Tomcat 临时目录；部署前验证主机 `/tmp` 为 `1777`、服务用户可写，并拒绝把 `/tmp`、`/var` 等共享系统目录作为图片数据父目录

### 文件上传安全

1. **文件类型校验**：旧版图片允许 JPEG、PNG、GIF、WebP；V2 另支持 MP3/M4A/OGG/WAV、MP4/WebM/MOV，并校验真实文件签名
2. **文件大小限制**：V2 图片最大25MB，音视频最大256MB；请求总大小最大300MB
3. **文件名安全**：使用 UUID 随机文件名，不保留用户名和客户端原始文件名；所有路径都限制在上传根目录内
4. **目录权限**：上传目录属主为 `baby-diary`，属组为 `www-data`，目录权限为 `2750`，图片文件权限为 `640`；后端负责写入，Nginx 只读取，避免 Java 进程以 root 写入业务文件，也避免静态图片因 Nginx 无权读取而返回 404
5. **资源归属**：手动相册和 AI 相册确认前都会重新校验 imageId 属于当前用户
6. **私有对象隔离**：`DIARY_OBJECT_PATH` 目录为 `0700`、对象为 `0600`，不得位于公开 `DIARY_FILE_PATH` 下
7. **签名访问**：V2 媒体 URL 包含资源、变体和过期时间签名，默认短时有效；锁定媒体在签发前要求二次验证

### AI配置安全

1. **密钥加密存储**：API Key 使用 AES-GCM 加密后写入 `ai_config.encrypted_api_key`
2. **环境密钥注入**：生产加密主密钥来自 `/etc/baby-diary/backend.env` 的 `AI_CONFIG_ENCRYPTION_KEY`
3. **前端不回显明文**：配置读取接口只返回是否已配置和脱敏占位
4. **最小化发送内容**：报告生成只发送必要日记文字元数据，不发送图片内容
5. **连接地址约束**：Base URL 只接受 HTTP/HTTPS 且不能包含账号、查询参数或片段；下游异常详情只写入受控日志，不直接回传前端

### 错误处理

- 统一异常处理：`GlobalExceptionHandler`
- 业务异常：`BusinessException`
- 错误码：`ErrorCode`枚举

---

## 质量保障架构

项目按脚本治理、单元/组件、真实 MySQL 集成、三浏览器 E2E、打包预发布和生产冒烟分层验证。`scripts/verify.sh` 聚合脚本、后端和前端门禁；Playwright 使用 MySQL 8.4、Redis 与仓库内回环 Node AI Mock 隔离运行；`compose.staging.yaml` 使用发布 Dockerfile 验证容器健康、安全头、ZAP 和 k6。脚本测试还覆盖 Nginx 预检顺序、真实 Actuator 状态、systemd 私有临时目录及共享目录权限保护。CI 的第三方 Actions 使用完整提交 SHA 固定，版本注释供 Dependabot 识别和更新，避免可变标签被重写后进入构建链路。

后端 JaCoCo 最低要求为 Lines 45%、Branches 28%；前端 Vitest 最低要求为 Lines 50%、Statements 45%、Functions 30%、Branches 30%。CI 将脚本、后端、前端、E2E、供应链和 DAST 拆为独立任务，周期性能工作流另以 50 个合成用户、峰值 10 VU 验证核心读路径。

测试环境不得加载生产用户数据。AI 自动化只访问仓库内回环 Node Mock，真实提供商仅允许在受控预发布环境通过 `scripts/smoke-ai-provider.sh` 冒烟。完整风险矩阵、真机清单和发布门禁见 [测试与发布验收方案](测试与发布验收方案.md)。
